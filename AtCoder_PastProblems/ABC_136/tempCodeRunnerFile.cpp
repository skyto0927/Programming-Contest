left.add(Y[i],1);
        // right.add(Y[i],-1);

        // mint lu = modpow((mint)2, left.sum(Y[i]+1,N+1)) - 1;
        // mint ld = modpow((mint)2, left.sum(1,Y[i])) - 1;
        // mint ru = modpow((mint)2, right.sum(Y[i]+1,right.M)) - 1;
        // mint rd = modpow((mint)2, right.sum(1,Y[i])) - 1;
        // ans += modpow((mint)2, N-1);
        // ans += lu*rd + ld*ru + lu*rd*(ld+ru) + ld*ru*(lu+rd) + lu*ld*ru*rd;
        // cout << lu << ", " << ld << ", " << ru << ", " << rd << endl;