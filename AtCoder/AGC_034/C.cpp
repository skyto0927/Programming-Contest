#include <iostream>
#include <stdio.h>
#include <math.h>
#include <string>
using namespace std;


int main(){
    int N, X;
    scanf("%d %d", &N, &X);

    int b[N], l[N], u[N];
    for (int i=0; i<N; i++){
        scanf("%d %d %d", &b[N], &l[N], &u[N]);
    }

    return 0;
}